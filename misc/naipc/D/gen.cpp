#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(0);
    srand(time(0));
    int n = rand() % 1000000;
    int p = rand() % 1000000;
    int q = rand() % 1000000;
    cout << n << ' ' << p << ' ' << q << '\n';
}

