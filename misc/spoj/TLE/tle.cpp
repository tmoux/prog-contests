#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const int maxn = 51, maxm = 16, M = 1e9;
int n, m, dp[maxn][1 << maxm];

void solve() {
    cin >> n >> m;
    memset(dp,-1,sizeof(dp));

}

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    int t; cin >> t;
    while (t--) solve();

    return 0;
}
	

