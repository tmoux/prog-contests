#include <bits/stdc++.h>
using namespace std;
using ll = long long;

const int maxn = 5005;
int N, K;
ll A[maxn];

void solve() {
    cin >> N >> K;
    for (int i = 0; i < N; i++) {
        cin >> A[i];
    }

}

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    int t; cin >> t;
    while (t--) {
        solve();
    }
}
