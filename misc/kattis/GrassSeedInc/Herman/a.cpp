#include <bits/stdc++.h>
using namespace std;
using ll = long long;

const double PI = 4*atan(1);

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    int r; cin >> r;
    cout << fixed << setprecision(10);
    cout << (r*r*PI) << '\n';
    cout << (r*r*2.0) << '\n';
}
