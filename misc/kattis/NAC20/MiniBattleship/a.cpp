#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int n, k;
char grid[5][5];

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cin >> grid[i][j];
        }
    }
}

