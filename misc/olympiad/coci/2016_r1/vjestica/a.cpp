#include <bits/stdc++.h>
using namespace std;
using ll = long long;

//3^N dp: at each stage, add to current, or start new
//We treat each subset independently.
//For each subset imagine, what's the longest prefix
//for all of those

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);
}

