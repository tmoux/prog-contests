#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    int L; cin >> L;
    double d = L/3.0;
    cout << fixed << setprecision(10) << (d*d*d) << '\n';
}

