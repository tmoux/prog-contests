#include <bits/stdc++.h>
using namespace std;
using ll = long long;

const int maxn = 1e5+5;
int n;
ll a[maxn];

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    ll ts, tf, t;
    cin >> ts >> tf >> t;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
}

