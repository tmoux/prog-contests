#include <bits/stdc++.h>
using namespace std;
using ll = long long;

const int maxn = 5e5+5;
int m, k, n, s, a[maxn], b[maxn];

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(0);
    cin >> m >> k >> n >> s;
    for (int i = 0; i < m; i++) cin >> a[i];
    for (int i = 0; i < s; i++) cin >> b[i];
        
}

