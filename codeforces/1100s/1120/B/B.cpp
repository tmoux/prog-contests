#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(0);
    int n; cin >> n;
    string a, b; cin >> a >> b;
    vector<int> diff(n);
    for (int i = 0; i < n; i++) diff[i] = (int)b[i] - (int)a[i];
        
}

