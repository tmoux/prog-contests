#include <bits/stdc++.h>
using namespace std;
using ll = long long;

const int M = 1e9+7;
map<char,int> cnt;

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    string s; cin >> s;
    n = s.size();
    for (char c: s) cnt[c]++;
    int q; cin >> q;
    while (q--) {
        int x, y; cin >> x >> y;
                                                                
    }


    return 0;
}

