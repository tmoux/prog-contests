#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    int n, m, k; cin >> n >> m >> k;
    set<pair,int,int>> s;
    for (int i = 0; i < k; i++) {
        int x, y; cin >> x >> y;
        s.insert({x,y});
    }

}

