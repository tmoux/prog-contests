#include <bits/stdc++.h>
using namespace std;

int main() {
    int T; cin >> T;
    while (T--) {
        string s; cin >> s;
        int d = (s[0]-'0') + (s[2]-'0');
        cout << d << '\n';
    }
}
